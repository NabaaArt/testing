import kotlin.math.abs

/**
 * given //setting requirements
 *when// operation to be teseted
 *then // assertation to have the right result
 * **/


fun isEven1(numberToCheck:Int ):Boolean{
    return numberToCheck %2==0
}


fun sumTwoNumbers2 (firstNumber :Int, secondNumber: Int ): Int {
    return firstNumber + secondNumber
}